name: build .NET and deploy

on:
  push:
    branches: 
      - main
      - devops/ci-cd
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
    # -------- 1 PHASE --------
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build solution
        run: dotnet build --configuration Release --no-restore

      - name: Run tests
        run: dotnet test --configuration Release --no-build --verbosity normal
      
      - name: Publish
        run: dotnet publish -c Release
      
    # -------- 2 PHASE --------

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}
      
      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/CareerPoint:latest src
      
      - name: Push Docker image
        run: docker push ${{ secrets.DOCKER_HUB_USERNAME }}/CareerPoint:latest
    
    # -------- 3 PHASE --------

      # - name: Deploy to server
      #   uses: appleboy/ssh-action@v1.0.0
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USERNAME }}
      #     password: ${{ secrets.SSH_KEY }}
      #     port: 49478
      #     script: |
      #       docker pull ${{ secrets.DOCKER_USERNAME }}/CareerPoint:latest
      #       docker stop CareerPoint || true
      #       docker rm CareerPoint || true
      #       docker run -d -p 5000:80 --name CareerPoint ${{ secrets.DOCKER_USERNAME }}/CareerPoint:latest



      # - name: Create file
      #   run: echo "Test" >> test.txt
      
      # - name: Pause for inspection (debug)
      #   run: sleep 600